let theme = {
  breaks: ["425px", "715px", "905px", "1280px"],
};

theme.breakpoints = {
  narrow: theme.breaks[0],
  small: theme.breaks[1],
  medium: theme.breaks[2],
  standard: theme.breaks[3],
};

export default theme;
